[build]
builder = "nixpacks"
buildCommand = "npm install"

[deploy]
startCommand = "npm rebuild better-sqlite3 --build-from-source && npm start"
healthcheckPath = "/api/health"
healthcheckTimeout = 60
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
